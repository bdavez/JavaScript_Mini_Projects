var age = prompt('What is your age?');
var maxAge = 80;

var snackage = 2 * 365;

var lifespan = maxAge - age;

var maxSnackage = snackage * lifespan

document.write("You will need ", maxSnackage, " snacks, to last you until the ripe old age of ", maxAge , "." )